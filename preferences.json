{
  "type": "object",
  "properties": {
    "useRGB": {
      "type": "boolean",
      "title": "Use RGB instead of Hexadecimal",
      "default": false
    },
    "prefix": {
      "type": "string",
      "title": "Hexadecimal Prefix",
      "enum": ["#", "0x"],
      "default": "#",
      "errorMessages": "The only valid options are # or 0x"
    }
  }
}